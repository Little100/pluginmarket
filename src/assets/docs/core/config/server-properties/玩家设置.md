# 玩家设置

本节介绍 `server.properties` 中与玩家相关的配置项，包括玩家数量、白名单、正版验证、权限等设置。

---

## max-players

| 属性 | 值 |
|------|-----|
| 键名 | `max-players` |
| 默认值 | `20` |
| 可选值 | `0-2147483647` |

服务器允许同时在线的最大玩家数量。

### 注意事项

- 此数值会显示在服务器列表中
- 实际可容纳的玩家数取决于服务器硬件性能
- OP 玩家可能可以在服务器满员时加入（取决于服务端实现）

### 推荐设置

| 服务器配置 | 推荐人数 |
|-----------|---------|
| 1GB 内存 | 10-15 |
| 2GB 内存 | 20-30 |
| 4GB 内存 | 40-60 |
| 8GB+ 内存 | 60-100+ |

> 💡 实际人数还受 CPU、网络带宽、插件数量等因素影响

---

## online-mode

| 属性 | 值 |
|------|-----|
| 键名 | `online-mode` |
| 默认值 | `true` |
| 可选值 | `true` / `false` |

是否启用正版验证。

- `true`：只允许正版玩家加入，通过 Mojang 服务器验证
- `false`：允许盗版玩家加入（离线模式）

### 重要警告

> ⚠️ **强烈建议保持 `online-mode=true`**

关闭正版验证会带来以下风险：
1. 任何人可以使用任意用户名加入
2. 玩家可以冒充他人身份
3. 无法使用 Mojang 的封禁系统
4. 可能违反 Minecraft EULA

### 群组服务器说明

如果使用 BungeeCord/Velocity 等代理：
- 代理服务器：`online-mode=true`
- 后端服务器：`online-mode=false`（由代理处理验证）

同时需要在后端服务器配置 `bungeecord: true`（Spigot）或相应的代理设置。

---

## white-list

| 属性 | 值 |
|------|-----|
| 键名 | `white-list` |
| 默认值 | `false` |
| 可选值 | `true` / `false` |

是否启用白名单。启用后，只有白名单中的玩家才能加入服务器。

### 管理白名单

使用以下命令管理白名单：

```
/whitelist on          # 启用白名单
/whitelist off         # 禁用白名单
/whitelist add <玩家>  # 添加玩家
/whitelist remove <玩家>  # 移除玩家
/whitelist list        # 查看白名单
/whitelist reload      # 重新加载白名单
```

白名单数据存储在 `whitelist.json` 文件中。

---

## enforce-whitelist

| 属性 | 值 |
|------|-----|
| 键名 | `enforce-whitelist` |
| 默认值 | `false` |
| 可选值 | `true` / `false` |

当白名单重新加载时，是否踢出不在白名单中的在线玩家。

- `true`：执行 `/whitelist reload` 时，不在白名单中的玩家会被踢出
- `false`：已在线的玩家不受影响，只影响新加入的玩家

---

## player-idle-timeout

| 属性 | 值 |
|------|-----|
| 键名 | `player-idle-timeout` |
| 默认值 | `0` |
| 可选值 | `0` 或正整数（分钟） |

玩家挂机超时时间（分钟）。超过此时间没有任何操作的玩家会被踢出。

- 设置为 `0` 禁用挂机踢出
- 推荐值：`15-30` 分钟

### 使用场景

- 防止玩家长时间挂机占用服务器资源
- 释放玩家槽位给活跃玩家
- 减少服务器负载

---

## op-permission-level

| 属性 | 值 |
|------|-----|
| 键名 | `op-permission-level` |
| 默认值 | `4` |
| 可选值 | `1-4` |

OP（管理员）的默认权限等级。

| 等级 | 权限 |
|------|------|
| 1 | 可以绕过出生点保护 |
| 2 | 可以使用单人游戏作弊命令（/give、/tp 等）和命令方块 |
| 3 | 可以使用多人游戏管理命令（/ban、/kick、/op 等） |
| 4 | 可以使用所有命令，包括 /stop、/reload 等 |

### 注意

- 此设置是 OP 的默认等级
- 可以在 `ops.json` 中为每个 OP 单独设置等级
- 建议日常管理使用等级 3，只给信任的管理员等级 4

---

## function-permission-level

| 属性 | 值 |
|------|-----|
| 键名 | `function-permission-level` |
| 默认值 | `2` |
| 可选值 | `1-4` |

函数（function）执行时的默认权限等级。

数据包中的函数会以此权限等级执行命令。

---

## enforce-secure-profile

| 属性 | 值 |
|------|-----|
| 键名 | `enforce-secure-profile` |
| 默认值 | `true` |
| 可选值 | `true` / `false` |

是否要求玩家拥有 Mojang 签名的公钥。

- `true`：只允许拥有有效公钥的玩家加入
- `false`：允许没有公钥的玩家加入

> 此设置与 1.19+ 的聊天签名系统相关

---

## hide-online-players

| 属性 | 值 |
|------|-----|
| 键名 | `hide-online-players` |
| 默认值 | `false` |
| 可选值 | `true` / `false` |

是否在服务器状态查询中隐藏在线玩家列表。

- `true`：服务器列表中不显示在线玩家名称
- `false`：正常显示在线玩家

---

## 玩家设置配置示例

### 公开服务器

```properties
max-players=50
online-mode=true
white-list=false
enforce-whitelist=false
player-idle-timeout=30
op-permission-level=4
hide-online-players=false
```

### 私密白名单服务器

```properties
max-players=20
online-mode=true
white-list=true
enforce-whitelist=true
player-idle-timeout=0
op-permission-level=4
hide-online-players=true
```

### 群组服务器后端

```properties
max-players=100
online-mode=false
white-list=false
enforce-whitelist=false
player-idle-timeout=0
op-permission-level=4
hide-online-players=false
```

### 安全配置

```properties
max-players=30
online-mode=true
white-list=true
enforce-whitelist=true
player-idle-timeout=15
op-permission-level=3
enforce-secure-profile=true
hide-online-players=true
```
