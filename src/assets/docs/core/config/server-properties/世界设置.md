# 世界设置

本节介绍 `server.properties` 中与世界相关的配置项，包括世界名称、种子、类型、视距等设置。

---

## level-name

| 属性 | 值 |
|------|-----|
| 键名 | `level-name` |
| 默认值 | `world` |
| 可选值 | 任意文件夹名 |

世界存档的文件夹名称。服务器会在此文件夹中存储世界数据。

### 注意事项

- 修改此值不会重命名现有世界，而是会创建/加载新的世界文件夹
- 文件夹名称建议使用英文，避免特殊字符
- 如果指定的文件夹不存在，服务器会自动创建新世界

### 多世界说明

原版服务端只支持一个主世界，下界和末地会自动创建在主世界文件夹内：
- `world/` - 主世界
- `world/DIM-1/` - 下界
- `world/DIM1/` - 末地

如需多个独立世界，需要使用 Multiverse 等插件。

---

## level-seed

| 属性 | 值 |
|------|-----|
| 键名 | `level-seed` |
| 默认值 | （空） |
| 可选值 | 任意字符串或数字 |

世界生成种子。相同的种子会生成相同的地形。

- 留空则随机生成种子
- 可以是数字或文本（文本会被转换为数字）

### 使用场景

- 复现特定的世界地形
- 与朋友分享有趣的种子
- 创建比赛用的相同地图

> ⚠️ 此设置仅在创建新世界时有效，对已存在的世界无效

---

## level-type

| 属性 | 值 |
|------|-----|
| 键名 | `level-type` |
| 默认值 | `minecraft\:normal` |
| 可选值 | 见下表 |

世界生成类型。

| 类型 | 说明 |
|------|------|
| `minecraft\:normal` | 标准世界 - 正常的地形生成 |
| `minecraft\:flat` | 超平坦 - 平坦的世界 |
| `minecraft\:large_biomes` | 巨型生物群系 - 生物群系面积增大 |
| `minecraft\:amplified` | 放大化 - 地形高度增加，更加险峻 |
| `minecraft\:single_biome_surface` | 单一生物群系 |

### 注意

- 1.19+ 版本需要使用带命名空间的格式（如 `minecraft\:normal`）
- 旧版本使用 `default`、`flat`、`largeBiomes`、`amplified`
- 放大化世界对服务器性能要求较高

---

## generator-settings

| 属性 | 值 |
|------|-----|
| 键名 | `generator-settings` |
| 默认值 | `{}` |
| 可选值 | JSON 格式的生成器设置 |

自定义世界生成器设置，主要用于超平坦世界。

### 超平坦世界示例

经典超平坦（草方块 + 泥土 + 基岩）：
```properties
generator-settings={"layers":[{"block":"bedrock","height":1},{"block":"dirt","height":2},{"block":"grass_block","height":1}]}
```

### 单一生物群系示例

```properties
level-type=minecraft\:single_biome_surface
generator-settings={"biome":"minecraft:plains"}
```

---

## max-world-size

| 属性 | 值 |
|------|-----|
| 键名 | `max-world-size` |
| 默认值 | `29999984` |
| 可选值 | `1-29999984` |

世界边界的最大半径（方块数）。玩家无法超出此范围。

### 推荐设置

| 场景 | 推荐值 | 说明 |
|------|--------|------|
| 默认 | `29999984` | 几乎无限大 |
| 限制探索 | `5000-10000` | 适合小型服务器 |
| 比赛地图 | `1000-3000` | 限制活动范围 |

### 注意

- 此设置限制的是世界边界，不是已生成区块
- 减小此值不会删除已生成的区块
- 可以配合 `/worldborder` 命令使用

---

## view-distance

| 属性 | 值 |
|------|-----|
| 键名 | `view-distance` |
| 默认值 | `10` |
| 可选值 | `2-32` |

服务器发送给客户端的区块距离（以区块为单位）。

### 性能影响

视距对服务器性能影响很大：

| 视距 | 加载区块数 | 说明 |
|------|-----------|------|
| 4 | 81 | 最低可用视距 |
| 8 | 289 | 低配服务器推荐 |
| 10 | 441 | 默认值 |
| 16 | 1089 | 高配服务器 |
| 32 | 4225 | 极高配置要求 |

### 推荐设置

- **低配服务器**：6-8
- **中配服务器**：10-12
- **高配服务器**：14-16

> 💡 Paper 服务端可以使用 `no-tick-view-distance` 实现更高的视距而不影响性能

---

## simulation-distance

| 属性 | 值 |
|------|-----|
| 键名 | `simulation-distance` |
| 默认值 | `10` |
| 可选值 | `2-32` |

实体和方块更新的模拟距离（区块）。超出此距离的区块不会进行游戏逻辑更新。

### 与 view-distance 的区别

- `view-distance`：玩家能看到多远
- `simulation-distance`：游戏逻辑（红石、生物 AI 等）在多远范围内运行

### 推荐设置

通常设置为小于或等于 `view-distance`：
- 低配服务器：4-6
- 中配服务器：8-10
- 高配服务器：10-12

---

## max-build-height

| 属性 | 值 |
|------|-----|
| 键名 | `max-build-height` |
| 默认值 | `256` |
| 可选值 | `16` 的倍数 |

世界的最大建筑高度。

> ⚠️ 1.18+ 版本中，世界高度由数据包控制，此设置可能不再有效

### 版本差异

- 1.17 及以前：Y 坐标范围 0-255
- 1.18+：Y 坐标范围 -64 到 319

---

## 世界设置配置示例

### 标准生存世界

```properties
level-name=world
level-seed=
level-type=minecraft\:normal
generator-settings={}
max-world-size=29999984
view-distance=10
simulation-distance=10
```

### 超平坦创造世界

```properties
level-name=flatworld
level-seed=
level-type=minecraft\:flat
generator-settings={}
max-world-size=10000
view-distance=12
simulation-distance=8
```

### 限制范围的比赛世界

```properties
level-name=arena
level-seed=12345
level-type=minecraft\:normal
generator-settings={}
max-world-size=3000
view-distance=8
simulation-distance=8
```

### 高性能优化配置

```properties
level-name=world
level-seed=
level-type=minecraft\:normal
generator-settings={}
max-world-size=29999984
view-distance=6
simulation-distance=4
```
